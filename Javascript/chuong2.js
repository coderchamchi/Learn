// var fullName = 'phan huy nam'; 
//tạo và gán giá trị biến fullName.

// alert(fullName); 
//gọi thông báo ra cái full name.

// console.log(fullName); 
//in ra ở tab console khi inspect.

// confirm('du tuoi chua?'); 
//hàm này để tạo ra thông báo cho lựa chọn, ở đây đang tạo ra thông báo hỏi đủ tuổi chưa và cho xác nhận, nó khác với alert đó nha
// check var là biết.

// prompt('nhap gi do di')
//hàm để user nhập vào gì đó tùy sau này mày design.

// setTimeout(function () {
//     alert('thong bao xuat hien sau 1s');
// }, 1000)
//hàm này để tạo ra một hàm trong nó, để chạy "1" đoạn code và sẽ chạy sau 1s( do con số 1000, 1000 nghĩa là 1000ms = 1s)

// setInterval(function () {
//     console.log('thong bao xuat hien sau 1s');
// }, 1000)
// hàm này tương tự với setTimeout, tuy nhiên nó chạy nhiều đoạn code và lặp đi lặp lại và cách nhau 1s

//Những hàm trên được gọi là built-in vì nó được js tạo sẵn, thực ra còn những cái khác nhưng đây ví dụ cơ bản thôi